# Exclude build artifacts — the Dockerfile re-builds inside the container
target/
*.class
*.jar
*.war

# IDE and OS files
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# Test reports
surefire-reports/

# Secrets — never bake into the image
.env
*-service-account.json
secrets/
